{% extends 'backoffice.html.twig' %}

{% block title %}New Enchere{% endblock %}

{% block body %}
    <div class="card">
        <div class="card-header">
            <h3 class="mb-0">Create new Enchère</h3>
        </div>
        <div class="card-body">
            {{ include('enchere/_form.html.twig') }}
            <a class="btn btn-link mt-3" href="{{ path('app_enchere_index') }}">Back to list</a>
        </div>
    </div>
{% endblock %}
