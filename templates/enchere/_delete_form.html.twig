<form method="post" class="d-inline" action="{{ path('app_enchere_delete', {'id': enchere.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ enchere.id) }}">
    <button class="btn btn-danger btn-sm">Delete</button>
</form>
