{% extends 'backoffice.html.twig' %}

{% block title %}Edit Enchere{% endblock %}

{% block body %}
    <div class="card">
        <div class="card-header">
            <h3 class="mb-0">Edit Enchère #{{ enchere.id }}</h3>
        </div>
        <div class="card-body">
            {{ include('enchere/_form.html.twig', {'button_label': 'Update'}) }}
            <div class="mt-3">
                <a class="btn btn-link" href="{{ path('app_enchere_index') }}">Back to list</a>
                <span class="ml-2">{{ include('enchere/_delete_form.html.twig') }}</span>
            </div>
        </div>
    </div>
{% endblock %}
