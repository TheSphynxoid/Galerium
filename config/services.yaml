# This file is the entry point to configure your own services.
# Files in the packages/ subdirectory configure your dependencies.

# Put parameters here that don't need to change on each machine where the app is deployed
# https://symfony.com/doc/current/best_practices.html#use-parameters-for-application-configuration
parameters:
    artistes_directory: '%kernel.project_dir%/public/uploads/artistes'
    # reCAPTCHA Configuration
    app.recaptcha.secret_key: '%env(RECAPTCHA_SECRET_KEY)%'
    app.recaptcha.site_key: '%env(RECAPTCHA_SITE_KEY)%'

services:
    # default configuration for services in *this* file
    _defaults:
        autowire: true      # Automatically injects dependencies in your services.
        autoconfigure: true # Automatically registers your services as commands, event subscribers, etc.
        bind:
            $twilioSid: '%env(TWILIO_SID)%'
            $twilioAuthToken: '%env(TWILIO_AUTH_TOKEN)%'
            $twilioFrom: '%env(TWILIO_FROM)%'
    # makes classes in src/ available to be used as services
    # this creates a service per class whose id is the fully-qualified class name
    App\:
        resource: '../src/'
        exclude:
            - '../src/DependencyInjection/'
            - '../src/Entity/'
    
    # Aucune configuration spécifique nécessaire pour RecaptchaVerifier
    # Il utilise maintenant une clé de test par défaut en développement

    App\Service\CommissionCalculator:
        arguments:
            $tauxCommission: 0.20

    # add more service definitions when explicit configuration is needed
    # please note that last definitions always *replace* previous ones

    App\Service\RedisService:
        arguments:
            $redis: '@snc_redis.default'

    App\EventSubscriber\EnchereSubscriber:
        tags:
            - { name: 'doctrine.event_listener', event: 'postPersist', method: 'postPersist' }
            - { name: 'doctrine.event_listener', event: 'postUpdate', method: 'postUpdate' }

    App\MessageHandler\EnchereEndHandler:
        tags: ['messenger.message_handler']
